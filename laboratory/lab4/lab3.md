function formatTitle($text, $maxLength = 20) {
    if (strlen($text) > $maxLength) {
        return substr($text, 0, $maxLength) . '...';
    }
    return $text;
}


function getCurrentGreeting() {
    $hour = (int) date("H");
    if (6 <= $hour && $hour < 12) {
        return "Доброго ранку";
    } elseif(12 <= $hour && $hour < 18) {
        return "Добрий день";
    } elseif (18 <= $hour && $hour < 24) {
        return "Добрий вечір";
    } else {
        return "Доброї ночі";
    }

Лабораторна робота №4 (2 години)

1.Якщо якась логіка потрібна двічі й більше вона виноситься в одне місце (наприклад, у функцію), а звідти вже викликається скільки завгодно разів.

2.Параметр це змінна, яку функція очікує отримати або вона описуєтся в її оголошенні
function formatTitle($text, $maxLength = 20) {
    $text і $maxLength — параметри
    
}
Аргумент це конкретне значення, яке передається під час виклику функції.
Якщо аргумент не вказати, буде використано значення за замовчуванням у нас це 20

formatTitle($taskTitle);        передано лише аргумент $taskTitle за замовчуванням $maxLength автоматично = 20
formatTitle($taskTitle, 30);    $taskTitle і 30 - аргументи

3.Тоді функція поверне значенням null.
тому $x = myFunc()  присвоїть $x значення null.

4.Область видимості це частина коду, де змінна існує й доступна. У PHP кожна функція має свою власну, локальну область видимості: змінні, оголошені всередині функції, не бачать змінних ззовні, і навпаки.

Ні, без global змінної звернутися до $taskTitle у тілі функції не можна.

5.Призначенна для обрізання тексту приймає три оргумента substr($text, 0, $maxLength)  вхідний рядок ,початок з 0-з якого символа почати вирізання та $maxLength скільки символів вирізати